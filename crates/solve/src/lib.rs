use constants::QsblTable;
use flow::Flow;
use repr::Repr;

pub struct Profile;

pub fn compute_profile(_table: QsblTable, _repr: Repr) -> Profile {
    todo!()
}

pub fn apply_profile(_repr: Repr, _profile: Profile) -> Flow {
    todo!()
}
